name: lab_cli
description: CLI do QuantLab — atualização de dados, análise, oportunidades, hipóteses e relatórios.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  googleapis_auth: ^1.6.0
  http: ^1.2.0
  quant_core: ^0.1.0
  quant_engine: ^0.1.0
  quant_market_data: ^0.1.0
  quant_stats: ^0.1.0

dev_dependencies:
  image: ^4.2.0
  test: ^1.25.0
