name: quant_core
description: Núcleo de domínio do QuantLab — entidades, value objects e portas. Zero dependências externas.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dev_dependencies:
  test: ^1.25.0
